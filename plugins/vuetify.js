import Vue from 'vue'
import Vuetify, {
    VList, VListTile, VListTileAction, VListTileContent, VListTileTitle
} from 'vuetify/lib'

Vue.use(Vuetify, {
    components: {
        VList, VListTile, VListTileAction, VListTileContent, VListTileTitle
    }
});
